export type VideoDownloadsProps = { state: CompleteState };

export default function VideoDownloads({ state }: VideoDownloadsProps): JSX.Element {
	return (
		<h1 className="text-2xl">Video Downloads</h1>
	);
}
